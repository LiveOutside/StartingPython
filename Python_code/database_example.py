import csv
#
# with open('../text_files/ikea.csv', encoding='utf-8') as file:
#     reader = csv.DictReader(file, delimiter=';')
#     expensive = sorted(reader, key=lambda x: int(x['price']))
# for i in expensive[:10]:
#     print(i)
